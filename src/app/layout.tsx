import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Temp Link",
  description:
    "TempLink lets you connect in real-time through secure, peer-to-peer messaging. No sign-ups. No traces. Just ephemeral conversations.",
  authors: [
    { name: "TempLink", url: "templink.chat" },
    { name: "Mario Alfredo Jorge", url: "https://github.com/mario-aj" },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
